# 知识体系图解

> 🏠 [项目首页](../README.md) | 📚 [文档中心](./README.md) | ⬅ [算法速查](./05-算法速查.md) | 📍 知识体系图解 | ➡ [练习与自检](./07-练习与自检.md)

---

本文档通过 6 种软件工程图，全方位展示 Python 数据挖掘项目的知识体系、模块关系、类结构、数据流转、状态变迁和部署架构。所有图均基于项目实际结构绘制。

---

## 一、用例图（Use Case Diagram）

展示学习者与贡献者两大角色与系统之间的交互关系。

```mermaid
graph TB
    ActorLearner([学习者])
    ActorContributor([贡献者])

    UC1[浏览模块目录]
    UC2[运行算法代码]
    UC3[查看可视化结果]
    UC4[做练习与自检]
    UC5[查询算法速查]
    UC6[阅读学习路线]
    UC7[查看数据文件说明]

    UC8[添加新模块]
    UC9[修复算法代码]
    UC10[编写单元测试]
    UC11[更新项目文档]
    UC12[优化可视化效果]
    UC13[补充示例数据]

    ActorLearner --> UC1
    ActorLearner --> UC2
    ActorLearner --> UC3
    ActorLearner --> UC4
    ActorLearner --> UC5
    ActorLearner --> UC6
    ActorLearner --> UC7

    ActorContributor --> UC8
    ActorContributor --> UC9
    ActorContributor --> UC10
    ActorContributor --> UC11
    ActorContributor --> UC12
    ActorContributor --> UC13

    UC1 -.->|00-09模块| UC2
    UC2 -.->|matplotlib| UC3
    UC5 -.->|docs/05-算法速查| UC2
    UC4 -.->|docs/07-练习与自检| UC2
    UC9 -.->|tests/| UC10
    UC8 -.->|docs/| UC11
```

**说明：**

- **学习者**是主要角色，通过浏览 `00_数据挖掘导论/` 至 `09_应用领域/` 的模块目录学习知识，运行各 `.py` 文件查看算法效果，借助 `docs/07-练习与自检.md` 进行自我检测
- **贡献者**负责项目维护，包括在 `tests/` 下编写测试、更新 `docs/` 文档、修复源码 Bug 等
- 虚线表示用例之间的依赖关系，如浏览模块后才能运行代码，查看速查后定位到具体算法

---

## 二、组件图（Component Diagram）

展示 10 个功能模块的内部组件及模块间的知识依赖关系。

```mermaid
graph TB
    subgraph M00["00_数据挖掘导论"]
        C00A[数据挖掘导论.py]
    end

    subgraph M01["01_数据仓库与OLAP"]
        C01A[数据仓库基础.py]
        C01B[OLAP多维分析.py]
    end

    subgraph M02["02_数据探索与处理"]
        C02A[数据预处理.py]
        C02B[特征工程.py]
        C02C[数据可视化.py]
    end

    subgraph M03["03_回归分析"]
        C03A[01_线性回归.py]
        C03B[02_逻辑回归.py]
    end

    subgraph M04["04_分类算法"]
        C04A[K近邻算法.py]
        C04B[朴素贝叶斯算法.py]
        C04C[ID3/C45/CART决策树]
        C04D[SVM算法.py]
        C04E[半监督学习与迁移学习.py]
    end

    subgraph M05["05_模型评估与调优"]
        C05A[模型评估与调优.py]
        C05B[类别不平衡处理.py]
        C05C[可解释AI.py]
    end

    subgraph M06["06_集成学习"]
        C06A[集成学习.py]
        C06B[现代梯度提升.py]
    end

    subgraph M07["07_无监督学习"]
        C07A[KMeans聚类.py / 高级聚类.py]
        C07B[Apriori / FP_Growth / 序列模式挖掘]
        C07C[PCA / SVD推荐 / SVD图像压缩]
        C07D[异常检测.py]
    end

    subgraph M08["08_深度学习"]
        C08A[神经网络基础.py]
        C08B[CNN/SVM/RF文本分类]
        C08C[自编码器与VAE.py]
        C08D[对比学习与自监督学习.py]
        C08E[Transformer与注意力机制.py]
    end

    subgraph M09["09_应用领域"]
        C09A[NLP基础.py]
        C09B[时间序列分析.py]
        C09C[推荐系统.py]
        C09D[图与网络挖掘.py / 图神经网络.py]
        C09E[Web挖掘.py]
        C09F[流数据挖掘.py]
        C09G[联邦学习与隐私保护.py]
    end

    subgraph Infra["公共基础设施"]
        Utils[utils.py<br/>公共工具]
        Tests[tests/<br/>测试框架]
        Docs[docs/<br/>文档系统]
    end

    M00 --> M01
    M00 --> M02
    M01 --> M02
    M02 --> M03
    M02 --> M04
    M03 --> M04
    M03 --> M05
    M04 --> M05
    M04 --> M06
    M05 --> M06
    M02 --> M07
    M04 --> M08
    M07 --> M08
    M08 --> M09
    M04 --> M09
    M07 --> M09

    Utils -.->|字体配置/数据生成| M00
    Utils -.->|字体配置/数据生成| M02
    Utils -.->|字体配置/数据生成| M03
    Utils -.->|字体配置/数据生成| M04
    Utils -.->|字体配置/数据生成| M07
    Tests -.->|单元测试| M03
    Tests -.->|单元测试| M04
    Tests -.->|单元测试| M05
    Tests -.->|单元测试| M06
    Tests -.->|单元测试| M07
    Tests -.->|单元测试| M08
    Tests -.->|单元测试| M09
    Docs -.->|文档导航| M00
```

**说明：**

- 10 个模块按 CRISP-DM 流程排列，实线箭头表示知识依赖方向（前置→后继）
- `00_数据挖掘导论` 是入口模块，`02_数据探索与处理` 是多数算法的前置基础
- `04_分类算法` 和 `07_无监督学习` 是核心枢纽，分别向 `08_深度学习` 和 `09_应用领域` 辐射
- `utils.py` 为多个模块提供中文字体配置和数据生成函数，`tests/` 覆盖模块 03-09 的单元测试

---

## 三、类图（Class Diagram）

展示项目中核心算法的类结构、继承关系和关键方法。

```mermaid
classDiagram
    class SimpleLinearRegression {
        +fit(X, y)
        +predict(X)
        +score(X, y)
    }

    class LinearRegressionGD {
        +fit(X, y)
        +predict(X)
    }

    class LogisticRegressionGD {
        -lr: float
        -n_iters: int
        -weights: ndarray
        -bias: float
        +fit(X, y)
        +predict(X)
        +predict_proba(X)
    }

    class SoftmaxRegression {
        +fit(X, y)
        +predict(X)
        +predict_proba(X)
    }

    class KMeansManual {
        -n_clusters: int
        -max_iters: int
        -centroids: ndarray
        +fit(X)
        +predict(X)
    }

    class DBSCANManual {
        -eps: float
        -min_samples: int
        +fit(X)
        +predict(X)
    }

    class BaggingManual {
        -n_estimators: int
        -base_estimator
        +fit(X, y)
        +predict(X)
    }

    class AdaBoostManual {
        -n_estimators: int
        -learning_rate: float
        +fit(X, y)
        +predict(X)
    }

    class Perceptron {
        -lr: float
        -n_iters: int
        +fit(X, y)
        +predict(X)
    }

    class MLP {
        -layers: list
        -lr: float
        +fit(X, y)
        +predict(X)
        -forward(X)
        -backward(X, y)
    }

    class AutoencoderManual {
        -encoding_dim: int
        -lr: float
        +fit(X)
        +encode(X)
        +decode(X)
        +predict(X)
    }

    class DenoisingAutoencoder {
        -noise_factor: float
        +fit(X)
        +add_noise(X)
    }

    class VAEManual {
        -latent_dim: int
        -lr: float
        +fit(X)
        +encode(X)
        +decode(Z)
        +sample(n_samples)
        -reparameterize(mu, log_var)
    }

    class SimCLRManual {
        -temperature: float
        -projection_dim: int
        +fit(X)
        +get_representations(X)
        -nt_xent_loss(z_i, z_j)
    }

    class MultiHeadAttention {
        -d_model: int
        -n_heads: int
        +forward(Q, K, V)
        -scaled_dot_product(Q, K, V)
    }

    class TransformerEncoderLayer {
        -d_model: int
        -nhead: int
        +forward(X)
        -feed_forward(X)
    }

    class LIMEManual {
        -n_samples: int
        -kernel_width: float
        +explain(instance, predict_fn)
        -generate_perturbations(instance)
        -fit_local_model(perturbations, predictions)
    }

    class SHAPManual {
        -n_samples: int
        +explain(instance, predict_fn, X_background)
        -shapley_value(feature, instance)
        -monte_carlo_sampling(instance)
    }

    class GCNManual {
        -n_hidden: int
        -n_classes: int
        +fit(X, A, y)
        +predict(X, A)
        -gcn_layer(X, A, W)
    }

    class GATManual {
        -n_heads: int
        -n_hidden: int
        +fit(X, A, y)
        +predict(X, A)
        -attention_mechanism(X, A)
    }

    class FedAvgManual {
        -n_clients: int
        -n_rounds: int
        +fit(X, y)
        -client_update(client_data)
        -server_aggregate(client_weights)
    }

    class DPSGDManual {
        -noise_multiplier: float
        -clip_bound: float
        +fit(X, y)
        -clip_gradients(grads)
        -add_noise(grads)
    }

    class optStruct {
        -X: ndarray
        -labelMat: ndarray
        -C: float
        -toler: float
        -kTup: tuple
        +calcEkK(k)
    }

    class treeNode {
        -name: str
        -numOccur: int
        -nodeLink: treeNode
        -children: dict
        +inc(num)
        +disp(ind)
    }

    class Graph {
        -adj_list: dict
        -n_nodes: int
        +add_edge(u, v, w)
        +bfs(start)
        +dfs(start)
        +pagerank()
    }

    class SlidingWindow {
        -window_size: int
        -data: list
        +add(item)
        +get_statistics()
    }

    class TextPreprocessor {
        +tokenize(text)
        +remove_stopwords(tokens)
        +clean(text)
    }

    SimpleLinearRegression <|-- LinearRegressionGD
    LinearRegressionGD <|-- LogisticRegressionGD
    LogisticRegressionGD <|-- SoftmaxRegression
    AutoencoderManual <|-- DenoisingAutoencoder
    AutoencoderManual <|-- VAEManual
    Perceptron <|-- MLP
    GCNManual <|-- GATManual
    FedAvgManual <|-- DPSGDManual
```

**说明：**

- **回归系列**：`SimpleLinearRegression` → `LinearRegressionGD` → `LogisticRegressionGD` → `SoftmaxRegression`，逐步从解析解到梯度下降，从二分类到多分类
- **深度学习系列**：`Perceptron` → `MLP` 构成神经网络基础；`AutoencoderManual` 派生出 `DenoisingAutoencoder` 和 `VAEManual`；`MultiHeadAttention` 与 `TransformerEncoderLayer` 组成 Transformer 架构
- **图神经网络**：`GCNManual` → `GATManual`，从基础图卷积到注意力图卷积
- **联邦学习**：`FedAvgManual` → `DPSGDManual`，在联邦聚合基础上增加差分隐私保护
- **可解释AI**：`LIMEManual` 和 `SHAPManual` 为独立类，分别实现局部可解释和 Shapley 值解释
- `optStruct` 为 SVM 的 SMO 优化数据结构，`treeNode` 为 FP-Growth 的树节点，`Graph` 为图挖掘基础类

---

## 四、数据流图（Data Flow Diagram）

展示数据从原始输入到最终输出的完整流转过程。

```mermaid
graph LR
    subgraph Input["数据输入层"]
        RawCSV[CSV/TXT原始数据<br/>datingTestSet2.txt<br/>lenses.txt 等]
        SklearnData[sklearn内置数据集<br/>iris/digits/make_*]
        SyntheticData[合成数据生成<br/>utils.generate_*_data]
    end

    subgraph Preprocess["数据预处理层<br/>02_数据探索与处理"]
        MissingVal[缺失值处理<br/>数据预处理.py<br/>replaceNaNWithMean]
        Normalization[标准化/归一化<br/>数据预处理.py<br/>StandardScaler]
        Encoding[编码转换<br/>特征工程.py<br/>OneHotEncoder]
        FeatureSelect[特征选择<br/>特征工程.py<br/>SelectKBest]
    end

    subgraph Model["模型训练层"]
        Regression[回归模型<br/>03_回归分析<br/>SimpleLinearRegression.fit]
        Classification[分类模型<br/>04_分类算法<br/>KNN/NB/SVM.fit]
        Ensemble[集成模型<br/>06_集成学习<br/>BaggingManual.fit]
        Unsupervised[无监督模型<br/>07_无监督学习<br/>KMeansManual.fit]
        DeepLearning[深度学习模型<br/>08_深度学习<br/>MLP/Autoencoder.fit]
    end

    subgraph Eval["评估与调优层<br/>05_模型评估与调优"]
        Metrics[评估指标<br/>模型评估与调优.py<br/>accuracy/F1/AUC]
        CrossVal[交叉验证<br/>模型评估与调优.py<br/>cross_val_score]
        Explain[可解释性<br/>可解释AI.py<br/>LIMEManual.explain]
        Imbalance[不平衡处理<br/>类别不平衡处理.py<br/>SMOTE]
    end

    subgraph Output["输出展示层"]
        Plot[可视化图表<br/>数据可视化.py<br/>plt.show]
        Report[文本报告<br/>utils.print_section<br/>评估指标输出]
        Predict[预测结果<br/>model.predict<br/>分类/回归值]
    end

    RawCSV --> MissingVal
    SklearnData --> Normalization
    SyntheticData --> Normalization
    MissingVal --> Normalization
    Normalization --> Encoding
    Encoding --> FeatureSelect

    FeatureSelect --> Regression
    FeatureSelect --> Classification
    FeatureSelect --> Ensemble
    FeatureSelect --> Unsupervised
    FeatureSelect --> DeepLearning

    Regression --> Metrics
    Classification --> Metrics
    Ensemble --> Metrics
    Unsupervised --> Metrics
    DeepLearning --> Metrics

    Metrics --> CrossVal
    Metrics --> Explain
    Metrics --> Imbalance

    CrossVal --> Plot
    CrossVal --> Report
    Explain --> Plot
    Metrics --> Predict
    Imbalance --> Classification
```

**说明：**

- **数据输入层**：三种数据来源——项目自带的 CSV/TXT 文件（如 `datingTestSet2.txt`）、sklearn 内置数据集（`digits`、`iris`）、以及 `utils.py` 中的合成数据生成函数
- **预处理层**：对应 `02_数据探索与处理/` 模块，依次经过缺失值处理、标准化、编码和特征选择
- **模型训练层**：5 类模型分别对应 `03-08` 模块，所有模型均接收预处理后的特征矩阵
- **评估层**：对应 `05_模型评估与调优/`，包含指标计算、交叉验证、可解释性分析和类别不平衡处理
- **输出层**：通过 `数据可视化.py` 和 `utils.print_section()` 输出图表与文本报告

---

## 五、状态图（State Diagram）

展示模型从初始化到部署的完整状态转换过程，包含正常流程和异常状态。

```mermaid
stateDiagram-v2
    [*] --> 初始化: 启动训练流程

    初始化 --> 数据加载: 配置参数<br/>random_state=42

    数据加载 --> 数据预处理: 读取数据集<br/>sklearn.load_*
    数据加载 --> 数据缺失异常: 文件不存在或格式错误

    数据缺失异常 --> 数据加载: 重新选择数据源

    数据预处理 --> 特征工程: 缺失值填充/标准化<br/>数据预处理.py
    数据预处理 --> 数据格式异常: 类型不匹配

    数据格式异常 --> 数据预处理: 修正数据格式

    特征工程 --> 模型构建: 特征选择/降维<br/>特征工程.py / PCA.py

    模型构建 --> 训练中: 调用model.fit()<br/>各算法模块

    state 训练中 {
        [*] --> 前向传播
        前向传播 --> 损失计算
        损失计算 --> 反向传播
        反向传播 --> 参数更新
        参数更新 --> 前向传播: epoch未结束
        参数更新 --> 收敛判断: epoch结束
        收敛判断 --> 前向传播: 未收敛
        收敛判断 --> [*]: 已收敛
    }

    训练中 --> 过拟合: 训练损失↓ 验证损失↑<br/>模型评估与调优.py检测
    训练中 --> 欠拟合: 训练损失未下降<br/>模型容量不足
    训练中 --> 发散: 损失值NaN/Inf<br/>学习率过大

    过拟合 --> 模型构建: 正则化/增加数据<br/>类别不平衡处理.py
    欠拟合 --> 模型构建: 增加模型复杂度<br/>特征工程.py
    发散 --> 初始化: 降低学习率<br/>重新初始化权重

    训练中 --> 评估阶段: 收敛完成

    评估阶段 --> 交叉验证: 模型评估与调优.py<br/>cross_val_score
    交叉验证 --> 可解释分析: LIMEManual/SHAPManual<br/>可解释AI.py
    可解释分析 --> 部署就绪: 指标达标

    评估阶段 --> 欠拟合: 指标不达标

    部署就绪 --> [*]: 保存模型/输出结果
```

**说明：**

- **正常流程**：初始化 → 数据加载 → 预处理 → 特征工程 → 训练中（迭代前向/反向传播）→ 收敛 → 评估 → 部署
- **训练中**的内部状态展示了每个 epoch 的前向传播→损失计算→反向传播→参数更新的循环
- **三种异常状态**：
  - **过拟合**：训练损失持续下降但验证损失上升，需通过正则化或数据增强解决
  - **欠拟合**：训练损失无法下降，需增加模型复杂度或改进特征
  - **发散**：损失变为 NaN/Inf，通常因学习率过大导致，需降低学习率重新训练
- 评估阶段包含交叉验证和可解释性分析，确保模型质量

---

## 六、部署图（Deployment Diagram）

展示本地开发环境的部署架构，包含 Python 环境、依赖包、项目文件和数据文件的关系。

```mermaid
graph TB
    subgraph Host["本地开发机器"]
        subgraph PythonEnv["Python 3.8+ 环境"]
            subgraph Venv["虚拟环境 venv/"]
                CorePkg["核心依赖<br/>numpy / pandas<br/>matplotlib / scikit-learn<br/>scipy"]
                OptionalPkg["可选依赖<br/>tensorflow / h5py<br/>jieba / networkx"]
                DevPkg["开发工具<br/>pytest / flake8<br/>mypy / coverage"]
            end
        end

        subgraph ProjectRoot["项目根目录 python-data-mining/"]
            MainCode["源码模块<br/>00_数据挖掘导论/<br/>01_数据仓库与OLAP/<br/>02_数据探索与处理/<br/>03_回归分析/<br/>04_分类算法/<br/>05_模型评估与调优/<br/>06_集成学习/<br/>07_无监督学习/<br/>08_深度学习/<br/>09_应用领域/"]
            Utils["公共工具<br/>utils.py"]
            TestData["测试目录<br/>tests/"]
            DocsDir["文档目录<br/>docs/"]
            Config["配置文件<br/>requirements.txt<br/>pytest.ini / .flake8<br/>mypy.ini / .gitignore"]
        end

        subgraph DataFiles["数据文件"]
            DataCSV["CSV/TXT数据<br/>datingTestSet2.txt<br/>lenses.txt<br/>bikeSpeedVsIq_*.txt"]
            DataZip["压缩数据<br/>digits.zip<br/>email.zip<br/>mushroom.zip<br/>kosarak.zip"]
            DataTxt["文本数据<br/>0_5.txt<br/>ex0.txt / ex2.txt<br/>sine.txt"]
        end
    end

    CorePkg --> MainCode
    OptionalPkg --> MainCode
    DevPkg --> TestData
    Utils --> MainCode
    MainCode --> DataCSV
    MainCode --> DataZip
    MainCode --> DataTxt
    Config --> Venv
    DocsDir --> MainCode
```

**说明：**

- **Python 环境**：推荐使用虚拟环境（venv）隔离项目依赖，核心依赖（numpy、pandas、matplotlib、scikit-learn、scipy）为必装项，tensorflow/jieba/networkx 为可选依赖
- **项目根目录**：包含 10 个功能模块（`00-09`）、公共工具 `utils.py`、测试目录 `tests/`、文档目录 `docs/` 和各类配置文件
- **数据文件**：分布在各模块子目录下，包括 CSV/TXT 格式的结构化数据、ZIP 压缩包和纯文本数据
- **配置文件**：`requirements.txt` 管理依赖清单，`pytest.ini`、`.flake8`、`mypy.ini` 分别配置测试、代码检查和类型检查规则
