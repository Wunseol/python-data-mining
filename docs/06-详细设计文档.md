# 详细设计文档

> 📚 [文档中心](./README.md) | ⬅ [架构设计](./05-架构设计文档.md) | 📍 详细设计 | ➡ [实施与开发](./07-实施与开发文档.md) | 🏠 [项目首页](../readme.md)

---

## 1. 模块详细设计

### 1.1 模块00：数据挖掘导论

**类图：**

```mermaid
classDiagram
    class 数据挖掘导论 {
        +print_mining_tasks()
        +print_crisp_dm()
        +data_types_overview()
        +euclidean_distance(x, y)
        +manhattan_distance(x, y)
        +minkowski_distance(x, y, p)
        +cosine_similarity(x, y)
        +jaccard_similarity(A, B)
        +pearson_correlation(x, y)
        +demo_distance_metrics()
        +print_applications()
    }
```

**数据流图：**

```mermaid
graph LR
    A["输入向量对"] --> B["距离度量函数"]
    B --> C["距离/相似度值"]
    C --> D["可视化输出"]
```

> 源码位置：[数据挖掘导论.py](../00_数据挖掘导论/数据挖掘导论.py)

---

### 1.2 模块01：数据仓库与OLAP

```mermaid
classDiagram
    class 数据仓库基础 {
        +demonstrate_warehouse_architecture()
        +demonstrate_multidimensional_model()
        +demonstrate_etl_process()
        +demonstrate_metadata()
        +compare_warehouse_vs_oltp()
    }

    class OLAP多维分析 {
        +build_data_cube()
        +demonstrate_olap_operations(sales_data)
        +demonstrate_olap_architectures()
        +demonstrate_precomputation_strategies()
        +demonstrate_attribute_oriented_induction()
    }
```

**ETL流程图：**

```mermaid
graph LR
    A["数据源"] --> B["Extract<br/>提取"]
    B --> C["Transform<br/>转换/清洗"]
    C --> D["Load<br/>加载"]
    D --> E["数据仓库"]
```

> 源码位置：[数据仓库基础.py](../01_数据仓库与OLAP/01_数据仓库基础/数据仓库基础.py) | [OLAP多维分析.py](../01_数据仓库与OLAP/02_OLAP多维分析/OLAP多维分析.py)

---

### 1.3 模块02：数据探索与处理

```mermaid
classDiagram
    class 数据预处理 {
        +create_sample_data()
        +handle_missing_values(df, strategy)
        +detect_outliers_iqr(df, column)
        +detect_outliers_zscore(df, column)
        +handle_outliers(df, column, method)
        +scale_features(df, columns, method)
        +encode_categorical(df, columns, method)
        +split_data(X, y, test_size, val_size)
    }

    class 特征工程 {
        +feature_selection_variance(X, threshold)
        +feature_selection_correlation(df, target_col)
        +feature_selection_mutual_info(X, y, k)
        +feature_selection_rfe(X, y, n_features)
        +feature_selection_model(X, y, top_k)
        +create_polynomial_features(X, degree)
        +create_interaction_features(df, columns)
        +transform_log(df, columns)
        +reduce_pca(X, n_components)
        +reduce_lda(X, y, n_components)
        +extract_tfidf(texts, max_features)
    }

    class 数据可视化 {
        +create_sample_data()
        +plot_basic_charts(df)
        +plot_statistical_charts(df)
        +plot_relation_charts(df)
        +plot_radar_chart(df)
        +plot_pair_plot(df)
    }
```

---

### 1.4 模块03：回归分析

```mermaid
classDiagram
    class 线性回归 {
        +SimpleLinearRegression：一元线性回归(最小二乘)
        +LinearRegressionGD：多元线性回归(梯度下降)
        +compare_regularization()：Ridge/Lasso/ElasticNet对比
    }

    class 逻辑回归 {
        +LogisticRegressionGD：逻辑回归(手动实现)
        +SoftmaxRegression：Softmax多分类回归
        +plot_roc_curve()：ROC曲线绘制
    }
```

> 源码位置：[01_线性回归.py](../03_回归分析/01_线性回归.py) | [02_逻辑回归.py](../03_回归分析/02_逻辑回归.py)

---

### 1.5 模块04：分类算法

```mermaid
classDiagram
    class K近邻算法 {
        +classify()：KNN分类
        +手写数字识别案例
    }

    class 朴素贝叶斯 {
        +trainNB0()：训练
        +classifyNB()：分类
        +垃圾邮件分类案例
    }

    class 决策树 {
        +createTree()：递归建树
        +ID3/C4.5/CART三种实现
        +CART可视化GUI
    }

    class SVM {
        +smoSimple()：SMO算法
        +手写数字识别案例
    }

    class 半监督学习 {
        +自训练/协同训练/标签传播
        +TrAdaBoost迁移学习
    }
```

---

### 1.6 模块05：模型评估与调优

```mermaid
classDiagram
    class 模型评估与调优 {
        +分类评估：accuracy/precision/recall/F1/AUC
        +回归评估：MSE/R²
        +聚类评估：轮廓系数
        +交叉验证：K-Fold
        +网格搜索：GridSearchCV
        +学习曲线
    }

    class 类别不平衡处理 {
        +随机过采样
        +SMOTE合成样本
        +欠采样
        +代价敏感学习
    }
```

> 源码位置：[01_模型评估与调优.py](../05_模型评估与调优/01_模型评估与调优.py) | [02_类别不平衡处理.py](../05_模型评估与调优/02_类别不平衡处理.py)

---

### 1.7 模块07：无监督学习

```mermaid
classDiagram
    class 聚类分析 {
        +KMeans(手写+sklearn)
        +DBSCAN
        +层次聚类
        +GMM
        +谱聚类
    }

    class 关联规则挖掘 {
        +Apriori算法
        +FP-Growth算法
        +序列模式挖掘
    }

    class 降维与矩阵分解 {
        +PCA主成分分析
        +SVD推荐系统
        +SVD图像压缩
    }

    class 异常检测 {
        +Z-Score
        +孤立森林
        +LOF
        +One-Class SVM
    }
```

---

### 1.8 模块08：深度学习

```mermaid
classDiagram
    class 神经网络基础 {
        +Perceptron：感知机
        +MLP：多层感知机
        +forward()：前向传播
        +backward()：反向传播
        +Dropout/BatchNorm
    }

    class 文本分类模型对比 {
        +CNN文本分类
        +SVM/逻辑回归/随机森林对比
    }
```

> 源码位置：[神经网络基础.py](../08_深度学习/01_神经网络基础/神经网络基础.py) | [CNN文本分类.py](../08_深度学习/02_文本分类模型对比/CNN文本分类.py)
